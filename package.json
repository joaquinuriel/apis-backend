{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "install": "bun prisma generate",
    "build": "bun bun src/index.ts --outdir=public"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "hono": "^3.10.2",
    "prisma": "^5.6.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@trunkio/launcher": "^1.2.7",
    "bun-types": "^1.0.7"
  }
}
